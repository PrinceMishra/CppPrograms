#include <iostream>
using namespace std;


int main()
{
    int t;
    cin >> t;
    if (t >= 1 && t <= 1000)
    {
        int a, b, m;
        while (t--)
        {
            cin >> a >> b >> m;
            if (a >= 1 && b >= 1 && a <= m && b <= m&& m >= 2 && (a != b))
            {
               

            }
        }   
    }
    return 0;
}
